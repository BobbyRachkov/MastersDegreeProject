<UserControl x:Class="MastersProject.App.Views.PfdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MastersProject.App.Views"
             mc:Ignorable="d" 
             xmlns:viewModels="clr-namespace:MastersProject.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:PfdViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock
                Foreground="White"
                FontSize="50"
                Text="{Binding AttitudeIndicator.RawPitch}"/>

            <TextBlock
                FontSize="50"
                Foreground="White"
                Text="{Binding AttitudeIndicator.RawRoll}"/>
            <TextBlock
                Foreground="White"
                FontSize="50"
                Text="{Binding AttitudeIndicator.Pitch,StringFormat=00.0°}"/>

            <TextBlock
                FontSize="50"
                Foreground="White"
                Text="{Binding AttitudeIndicator.Roll,StringFormat=00.0°}"/>
        </StackPanel>
    </Grid>
</UserControl>
