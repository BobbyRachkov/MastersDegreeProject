<UserControl x:Class="MastersProject.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MastersProject.App.ViewModels"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel
        Orientation="Vertical">
        <TabControl x:Name="AnimatedTabControl"
                    Style="{DynamicResource MahApps.Styles.TabControl.Animated}">
            <mah:MetroTabItem 
                Header="Raw value to Angle mapping">
                <Grid
                    Margin="-2 -3.5 0 0"
                    Height="300">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="460"/>
                        <ColumnDefinition Width="460"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ContentControl
                        Grid.Column="0"
                        Content="{Binding PitchSetup}"/>
                    <ContentControl
                        Grid.Column="1"
                        Content="{Binding RollSetup}"/>
                    <GroupBox
                        Grid.Column="2"
                        mah:HeaderedControlHelper.HeaderHorizontalContentAlignment="Center"
                        mah:HeaderedControlHelper.HeaderBackground="White"
                        BorderBrush="Black"
                        Margin="15 4 "
                        Header="Spatial Representation">
                        <Grid>
                            <Button
                            Visibility="Hidden"
                            Command="{Binding AddPointClick}"
                            Margin="5,5,0,0"
                            Width="100"
                            Height="50"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Left">
                                Add point
                            </Button>
                            <ContentControl
                            Height="202"
                            Width="202"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Content="{Binding DemoGraph}"/>
                        </Grid>
                    </GroupBox>
                </Grid>
            </mah:MetroTabItem>
            <mah:MetroTabItem 
                Header="Angle to Visualization mapping">
                <Grid
                    
                    Margin="-2 -3.5 0 0"
                    Height="300">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="460"/>
                        <ColumnDefinition Width="460"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ContentControl
                Grid.Column="0"
                Content="{Binding PitchSetup}"/>
                    <ContentControl
                Grid.Column="1"
                Content="{Binding RollSetup}"/>
                    <Grid
                        Grid.Column="2">
                        <TextBlock
                    FontSize="20"
                    Margin="0 7 0 0"
                    HorizontalAlignment="Center">
                    Spatial Representation
                        </TextBlock>
                        <Button
                    Visibility="Hidden"
                    Command="{Binding AddPointClick}"
                    Margin="5,5,0,0"
                    Width="100"
                    Height="50"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left">
                            Add point
                        </Button>
                        <ContentControl
                    Height="202"
                    Width="202"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Content="{Binding DemoGraph}"/>
                    </Grid>
                </Grid>
            </mah:MetroTabItem>
        </TabControl>
        <Rectangle
            Height="1"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            Margin="0 20"
            StrokeThickness="0.5"
            Stroke="Black"/>
    </StackPanel>
</UserControl>
